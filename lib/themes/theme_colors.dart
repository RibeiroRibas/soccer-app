import 'package:flutter/material.dart';

class ThemeColors {
  static const MaterialColor division = Colors.grey;
  static const Color principalPosition = Colors.orange;
  static const Color secondaryPosition = Colors.white;
  static const Color tertiaryPosition = Colors.pinkAccent;
  static const Color primary = Color.fromRGBO(0, 255, 66, 1);
}

final Map<int, Color> shades = {
  50: const Color.fromRGBO(0, 255, 66, 1)
};